version: "3.9"
services:
  first-redux-app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/redux-shoppingcart-app
    command: sh -c "cd redux-shoppingcart-app && npm start"
    ports:
      - "3000:3000"
    stdin_open: true
